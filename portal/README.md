# NewsNexus12Lite Portal

The portal is a Next.js App Router application. It uses native `fetch` in `src/lib/apiClient.ts` so API calls can include credentials for the Lite session cookie without coupling the client to Redux.
